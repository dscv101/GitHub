# Placeholder SOPS file.
# After generating an age key, run:
#   sops -e -i secrets.sops.yaml
# and fill in values below (encrypted by sops).
# RCLONE_CONFIG should contain a standard rclone.conf with a [b2-blazar] remote.
sops: PLACEHOLDER - run scripts/init-secrets.sh first

RESTIC_PASSWORD: "changeme"
B2_ACCOUNT_ID: "xxx"
B2_ACCOUNT_KEY: "xxx"
TAILSCALE_AUTHKEY: "tskey-xxx"
MOTHERDUCK_TOKEN: ""
GITHUB_TOKEN: ""
GHCR_USER: ""
GHCR_TOKEN: ""

# Environment file for restic backup service
restic_env: |
  RESTIC_PASSWORD=changeme
  RCLONE_CONFIG_B2_BLAZAR_TYPE=b2
  RCLONE_CONFIG_B2_BLAZAR_ACCOUNT=xxx
  RCLONE_CONFIG_B2_BLAZAR_KEY=xxx

# restic_env should contain environment variables for the backup service:
# RESTIC_PASSWORD=<password>
# RCLONE_CONFIG_B2_BLAZAR_TYPE=b2
# RCLONE_CONFIG_B2_BLAZAR_ACCOUNT=<B2_ACCOUNT_ID>
# RCLONE_CONFIG_B2_BLAZAR_KEY=<B2_ACCOUNT_KEY>
# This configures rclone to use B2 with the "b2-blazar" remote name.
