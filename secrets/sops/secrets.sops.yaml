# Placeholder SOPS file.
# After generating an age key, run:
#   sops -e -i secrets.sops.yaml
# and fill in values below (encrypted by sops).
# RCLONE_CONFIG should contain a standard rclone.conf with a [b2-blazar] remote.
sops: PLACEHOLDER - run scripts/init-secrets.sh first

RESTIC_PASSWORD: "changeme"
B2_ACCOUNT_ID: "xxx"
B2_ACCOUNT_KEY: "xxx"
TAILSCALE_AUTHKEY: "tskey-xxx"
MOTHERDUCK_TOKEN: ""
GITHUB_TOKEN: ""
GHCR_USER: ""
GHCR_TOKEN: ""

# RCLONE_CONFIG (binary) is managed separately by sops-nix as a file secret.
# To add it:
#   printf '%s\n' '[b2-blazar]\n type = b2\n account = ...\n key = ...' > rclone.conf
#   sops -e -i --input-type binary --output-type binary rclone.conf
#   Then move/rename into place and reference via sops-nix if desired.
