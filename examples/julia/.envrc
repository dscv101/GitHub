# Julia project .envrc example
# Copy this to your Julia project root

# Use the Julia development environment
use flake ..#devenv.julia

# Julia-specific environment variables
export JULIA_DEPOT_PATH="$PWD/.julia"
export JULIA_PROJECT="$PWD"
export JULIA_NUM_THREADS="auto"

echo "🔬 Julia development environment loaded!"
echo "Available commands: julia-init, julia-activate, julia-install, julia-test, julia-repl, julia-notebook"
