#!/usr/bin/env bash
# Rust project .envrc example
# Copy this to your Rust project root

# Use the Rust development environment
use flake ..#devenv.rust

# Rust-specific environment variables
export RUST_BACKTRACE=1
export CARGO_HOME="$PWD/.cargo"
export RUSTUP_HOME="$PWD/.rustup"
export SCCACHE_DIR="$PWD/.sccache"

echo "🦀 Rust development environment loaded!"
echo "Available commands: rust-init, rust-build, rust-test, rust-check, rust-format, rust-audit, rust-clean, rust-watch"
