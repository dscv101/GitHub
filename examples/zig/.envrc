#!/usr/bin/env bash
# Zig project .envrc example
# Copy this to your Zig project root

# Use the Zig development environment
use flake ..#devenv.zig

# Zig-specific environment variables
export ZIG_GLOBAL_CACHE_DIR="$PWD/.zig-cache"
export ZIG_LOCAL_CACHE_DIR="$PWD/zig-cache"

echo "⚡ Zig development environment loaded!"
echo "Available commands: zig-init, zig-init-lib, zig-build, zig-run, zig-test, zig-check, zig-format, zig-clean"
